name: CI

on:
  push:
    branches: ["master", "main", "wt/*"]
  pull_request:
    branches: ["master", "main"]

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: CI temporarily disabled
        run: echo "CI checks temporarily disabled"
