# Archive

This directory contains files that were archived during the Markdown hierarchy
consolidation. These files are one-time task artifacts, integration markers,
and other ephemeral documents that have been superseded by the canonical
`docs/` hierarchy.

They are retained here for audit trail and historical reference. New content
should not be added to this directory — all active documentation lives under
[`docs/`](../docs/).
